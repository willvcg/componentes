import { OnlyTextDirective } from './only-text.directive';

describe('OnlyTextDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyTextDirective();
    expect(directive).toBeTruthy();
  });
});
